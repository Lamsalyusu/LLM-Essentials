    Pipeline is a high level API that lets you use a pre trained model for a specific task with vey little code:
    Think of it as 

    Your text
    ↓
    pipeline()
    ↓
    Tokenizer
    ↓
    Model
    ↓
    Post-processing
    ↓
    Result

    Instead of manually doing all of these steps, pipeline() does them for you.

=========================================================================================

    We didnot explicitly write 
    tokenizer
    model
    input_ids
    attention_mask
    forward()
    softmax
    post-processing

==========================================================================================

when we write 
classifier - pipeline("sentiment-analysis)

hugging face roughly does:

                  pipeline
                     │
          ┌──────────┴──────────┐
          ↓                     ↓
      Tokenizer               Model
          │                     │
          └──────────┬──────────┘
                     ↓
                  Output
                     ↓
               Post-process

============================================================================================

Why is it called a pipeline?
Because there are multiple processing stages:

Input
 ↓
Preprocessing
 ↓
Model
 ↓
Postprocessing
 ↓
Output

============================================================================================
Different Pipelines Tasks

Hugging Face supports many tasks.
-----------------------------------------------------------
Sentiment analysis
pipeline("sentiment-analysis")
------------------------------------------------------------
Text classification
pipeline("text-classification")
------------------------------------------------------------
Text generation
pipeline("text-generation")
------------------------------------------------------------
Question answering
pipeline("question-answering")
------------------------------------------------------------
Summarization
pipeline("summarization")
------------------------------------------------------------
Translation
pipeline("translation")
------------------------------------------------------------
Named Entity Recognition
pipeline("ner")
------------------------------------------------------------
Zero-shot classification
pipeline("zero-shot-classification")
------------------------------------------------------------
There are also pipelines for speech, vision, image classification, object detection, etc.

============================================================================================
This is Text generation part 

from transformers import pipeline

generator = pipeline("text-generation")
result = generator(
    "Artificial intelligence is",
    max_new_tokens=50
)
print(result)

YOU CAN SPECIFY A MODEL ALSO 

generator = pipeline(
    "text-generation",
    model="gpt2"
)

pipeline
   ↓
GPT-2 tokenizer
   +
GPT-2 model

===========================================================================================
    
    "Artificial intelligence is"
                ↓
            tokenizer
                ↓
            token IDs
                ↓
        Transformer
                ↓
            logits
                ↓
        choose next token
                ↓
        next token
                ↓
        choose next token
                ↓
               ...

This is autoregressive generation

==============================================================================================
What is inside a pipeline:

                 pipeline()
                     │
                     ↓
              PREPROCESSING
                     │
                     ↓
                Tokenizer
                     │
                     ↓
               input_ids
               attention_mask
                     │
                     ↓
                  MODEL
                     │
                     ↓
                 logits
                     │
                     ↓
              POSTPROCESSING
                     │
                     ↓
                  result
==============================================================================================

DONT BECOME DEPENDENT ON pipeline() function this is just for inference 

==============================================================================================

How does a Transformer work?

The simplest way to think about a Transformer is:

A Transformer takes tokens, repeatedly lets them look at each other using attention, processes the information with FFNs, and finally predicts something.

"I love playing cricket"
          ↓
      Tokenizer
          ↓
   Token IDs
          ↓
    Embeddings
          ↓
 Positional information
          ↓
 ┌─────────────────────┐
 │ Transformer Block 1 │
 ├─────────────────────┤
 │ Transformer Block 2 │
 ├─────────────────────┤
 │ Transformer Block 3 │
 ├─────────────────────┤
 │         ...         │
 ├─────────────────────┤
 │ Transformer Block N │
 └─────────────────────┘
          ↓
      Final Norm
          ↓
       LM Head
          ↓
        Logits
          ↓
   Next-token prediction

A Transformer is an architecture made by stacking Transformer blocks, 
and each block contains attention, FFN, normalization, and residual connections.


Transformer
    │
    ├── Block 1
    │     ├── Attention
    │     ├── FFN
    │     ├── Norm
    │     └── Residuals
    │
    ├── Block 2
    │     ├── Attention
    │     ├── FFN
    │     ├── Norm
    │     └── Residuals
    │
    ├── Block 3
    │     └── ...
    │
    └── Block N

===============================================================================================================================================================
some of the transformer models

GPT ,BERT , INSTRUCTGPT, T5

some of the language models that are trained are:
1.Masked Language Modelling (MLM)
--> this approach randomly masks some tokens in the input and trains the model to predict the original tokens based on the surrounding context. 
This allows the model to learn bidirectional context (looking at words both before and after the masked word)

2.Casual Language Modelling (CLM)
--> this approach predicts the next token based on all previous tokens in the sequence. 
The model can only use context from the left (previous tokens) to predict the next token.

================================================================================================================================================================

Types of language models

In the Transformers library, language models generally fall into three architectural categories:

1.Encoder-only models (like BERT): These models use a bidirectional approach to understand context from both directions. They’re best suited for tasks that require deep understanding of text, such as classification, named entity recognition, and question answering.

2.Decoder-only models (like GPT, Llama): These models process text from left to right and are particularly good at text generation tasks. They can complete sentences, write essays, or even generate code based on a prompt.

2.Encoder-decoder models (like T5, BART): These models combine both approaches, using an encoder to understand the input and a decoder to generate output. They excel at sequence-to-sequence tasks like translation, summarization, and question answering.

==================================================================================================================================================================

transformer image how does it works

Using “I love cricket” → French:

"I love cricket"
      ↓
   Encoder
      ↓
Contextual representations
      ↓
   Decoder

Then the decoder works step by step:

Step 1:
<START>
  ↓
Masked Self-Attention
  ↓
Cross-Attention → looks at Encoder output
  ↓
Predicts "J'"

Step 2:
<START> J'
       ↓
Masked Self-Attention
       ↓
Cross-Attention → looks at Encoder output
       ↓
Predicts "aime"

Step 3:
<START> J' aime
       ↓
Masked Self-Attention
       ↓
Cross-Attention → looks at Encoder output
       ↓
Predicts "le"
============================================================================================================================================================

Context Length and Attention Span
-> context length refers to the maximum number of tokens (word or parts of words) that the LLM can process at once.

These capabilities are limited by several practical factors:
The model’s architecture and size
Available computational resources
The complexity of the input and desired output

in ideal world we can feed unlimited context to the model but hardware constraints and computational costs makes this impractical.

The context length is the maximum number of tokens the model can consider at once when generating a response.

==============================================================================================================================================================

The Art Of Prompting::::::::
When we pass information to LLMs, we structure our input in a way that guides the generation of the LLM toward the desired output. This is called prompting.
Since the model’s primary task is to predict the next token by analyzing the importance of each input token, the wording of your input sequence becomes crucial.

===============================================================================================================================================================

The Two-Phase Inference Process
let’s dive into how LLMs actually generate text. The process can be broken down into two main phases: prefill and decode

1.Prefill phase

Tokenization: Converting the input text into tokens (think of these as the basic building blocks the model understands)

Embedding Conversion: Transforming these tokens into numerical representations that capture their meaning

Initial Processing: Running these embeddings through the model’s neural networks to create a rich understanding of the context

2.Decode Phase

Attention Computation: Looking back at all previous tokens to understand context

Probability Calculation: Determining the likelihood of each possible next token

Token Selection: Choosing the next token based on these probabilities

Continuation Check: Deciding whether to continue or stop generation

This phase is memory-intensive because the model needs to keep track of all previously generated tokens and their relationships.
===============================================================================================================================================================

Understanding the token selection 
Model predicts scores → we control those scores → filter possible tokens → sample one token.

1. Raw Logits
The model first produces a score for every token in its vocabulary.
For example:

"cricket"  → 8.5
"football" → 7.2
"banana"   → 2.1
"computer" → 5.4

These are logits, not probabilities.

2. Temperature Scaling
Temperature controls how confident/random the models choices are.
conceptually:

Low temperature
      ↓
More focused / predictable

High temperature
      ↓
More random / diverse

mathematically:
logits/temperature
then softmax converts them into probalilities

Temperature = 0.2

cricket   → 0.90
football  → 0.08
banana    → 0.001

Temperature = 1.5

cricket   → 0.40
football  → 0.30
banana    → 0.10

3.Top-k Filtering
Only consider the K most probable tokens.
suppose:
cricket   40%
football  30%
tennis    15%
banana     5%
car        3%

if top_k = 3:

we keep:
cricket
football
tennis

and throw away the rest.

4.Top-p sampling 
Now we use a probability threshold instead of a fixed number.

Suppose:

cricket   40%
football  30%
tennis    15%
banana     5%

If:
top_p = 0.90
we keep tokens until their cumulative probability reaches 90%:
40 + 30 + 15 + 5 = 90%

Top-k → fixed number of tokens

Top-p → variable number of tokens based on probability


5.Selected Token

Finally, the model samples/selects one token from the remaining candidates.

For example:

cricket   40%
football  30%
tennis    15%
banana     5%

It might select:

football

Then that token is added to the sequence:

"I love playing football"

And the whole process happens again to predict the next token.


Transformer
    ↓
Raw Logits
    ↓
Temperature
    ↓
Top-k
    ↓
Top-p
    ↓
Sample
    ↓
Selected Token
    ↓
Feed token back into model
    ↓
Predict next token
    ↓
Repeat...

top-k and top-p are optional sampling controls, and implementations don't necessarily require using both. In practice, you can use temperature alone, top-p, top-k, or combinations depending on the generation strategy.

Temperature controls the randomness, top-k limits the candidate pool by count, and top-p limits it by cumulative probability.

=======================================================================================

Managing Repetition: Keeping Output Fresh

One common challenge with LLMs is their tendency to repeat themselves - much like a speaker who keeps returning to the same points. To address this, we use two types of penalties:

Presence Penalty: A fixed penalty applied to any token that has appeared before, regardless of how often. This helps prevent the model from reusing the same words.

Frequency Penalty: A scaling penalty that increases based on how often a token has been used. The more a word appears, the less likely it is to be chosen again.

=======================================================================================
